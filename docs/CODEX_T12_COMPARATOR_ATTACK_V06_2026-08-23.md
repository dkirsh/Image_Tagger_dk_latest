VERDICT: BROKEN

## 2. Hash Reviewed And Scope Confirmation

Reviewed committed hash: `d2149736e81885adf9b768317c916dd0c498748f`.

Checkout used: `/tmp/review_v06`, detached at that hash. `git status --short` was empty after the review. Findings below are against `/tmp/review_v06/loop/run_loop_compare.py`, `loop/tests/test_run_loop_compare.py`, the two loop schemas, and `loop/fixtures/e2e_objects_scene.json`. I did not review the live working tree and did not make any source changes. `orchestrate.py` and `tests/test_orchestrate.py` remain out of scope for findings.

Note: one broad repository search returned out-of-scope paths; I discarded that output and do not rely on it here.

## 3. Round-4 And Round-5 Regression Results

Baseline test command:

```
PYTHONPATH=. python3 loop/tests/test_run_loop_compare.py
```

Observed output: `39/39 passed`. `pytest` was not installed, so the file's stdlib runner executed the tests.

Custom v0.6 packet regressions were generated by `/tmp/codex_v06_attacks.py`; full stdout is in `/tmp/codex_v06_attacks.out`.

Round-4 fixes still closed under custom attacks:

| Case | Exact input pointer | Observed output |
|---|---|---|
| `r4_null_object_bbox` | target object has `"object_bbox": null`; good room echo | `code=0`; `verdict CONTINUE score=0.0`; `position_unverified:["o1"]` |
| `r4_target_openings_null` | target has `"openings": null` | `code=2`; `REFUSED: target scene openings is null ...` |
| `r4_target_objects_null` | target has `"objects": null` | `code=2`; `REFUSED: target scene objects is null ...` |
| `r4_room_apertures_null` | room has `"apertures": null` | `code=2`; `REFUSED: room.json apertures is null ...` |
| `r4_room_furniture_null` | room has `"furniture": null` | `code=2`; `REFUSED: room.json furniture is null ...` |
| `r4_render_category_int` | target category `"7"`, render category `7` | `code=0`; `CONTINUE score=0.4`; `matched:[]`; `missing:["7"]`; `extra:["<unreadable-category:int>"]` |
| `r4_ap_id_trailing_newline` | room aperture id `"ap0\n"` | `code=0`; `CONTINUE score=0.0`; `identity.mode:"multiset_fallback"` |
| `r4_negative_iter` | manifest `"iter": -1` | `code=2`; `REFUSED: packet.json iter must be >= 0 ...` |

Round-5 fixes:

| Case | Exact input pointer | Observed output |
|---|---|---|
| `f2_unreadable_fallback` | room apertures: `[{"kind":"door","wall":"north"},{"kind":7,"wall":"north"},{"kind":"window","wall":null}]` | `code=0`; `CONTINUE score=0.133333`; extras `["<non-string:int>->north","window-><non-string:NoneType>"]`; readable door matched; strict did not agree |
| `f3_target_nested_nan` | target ignored nested value `NaN` | `code=2`; `REFUSED: target scene: non-finite JSON constant NaN refused (RFC 8259)` |
| `f3_target_array_inf` | target bbox array contains `Infinity` | `code=2`; `REFUSED: target scene: non-finite JSON constant Infinity refused (RFC 8259)` |
| `f3_target_object_neginf` | target evidence contains `-Infinity` | `code=2`; `REFUSED: target scene: non-finite JSON constant -Infinity refused (RFC 8259)` |
| `f3_room_ignored_nan` | room ignored aperture field `NaN` | `code=2`; `REFUSED: room.json: non-finite JSON constant NaN refused (RFC 8259)` |
| `f3_room_furniture_inf` | room `_source_bbox` contains `Infinity` | `code=2`; `REFUSED: room.json: non-finite JSON constant Infinity refused (RFC 8259)` |
| `f3_room_top_neginf` | room top-level ignored field `-Infinity` | `code=2`; `REFUSED: room.json: non-finite JSON constant -Infinity refused (RFC 8259)` |
| `f3_packet_ignored_nan` | manifest ignored field `NaN` | `code=2`; `REFUSED: packet.json: non-finite JSON constant NaN refused (RFC 8259)` |

No round-4 or round-5 regression attack produced a wrong strict `BELOW_THRESHOLD`.

## 4. `_disp` Trace And Attacks

Static trace command: `python3 /tmp/codex_v06_trace.py`; full stdout is in `/tmp/codex_v06_trace.out`.

Trace result:

```
_disp call sites:
  line 365: _disp(a.get('kind', 'unknown')) | inside sorted() argument
  line 366: _disp(a.get('wall', 'unknown')) | inside sorted() argument
  line 362: _disp(a.get("wall", "unknown")) | inside Dict literal
  line 364: _disp(a.get("kind", "unknown")) | inside Dict literal
  line 382: _disp(k) | append to unreadable display list, later sorted at lines 399-400
  line 382: _disp(w) | append to unreadable display list, later sorted at lines 399-400
```

Matching-path assessment:

- Exact opening comparisons use raw `a.get("kind")` and `a.get("wall")` at `/tmp/review_v06/loop/run_loop_compare.py:358`; `_disp` is only used to populate mismatch/extra report fields.
- Fallback opening matching admits only usable string `(kind, wall)` pairs into `have` at lines 375-377. Unreadable values go to `unreadable` at line 382 and never enter `have`.
- The membership test `if w in avail` at line 389 uses expected walls against raw render string values in `have`, not `_disp` output.
- Score components use only list lengths at lines 493-498. `_disp` content does not affect score denominators or the `BELOW_THRESHOLD` decision.

Finding: `_disp` output still reaches sort ordering in the verdict artifact. In exact mode, `_disp` is inside the `sorted(...)` argument for `extra_render_apertures` at lines 365-366. In fallback mode, unreadable display strings built with `_disp` at line 382 are included in the sorted `extra` list at lines 399-400.

Attack `disp_sort_key` exact input:

```
target.json:
{"image_id":"f2","openings":[{"bbox_xywh":[0.5,0.5,0.1,0.3],"kind":"door"}]}

room.json:
{"apertures":[{"kind":7,"wall":"north"},{"kind":null,"wall":"north"},{"kind":"zzz","wall":"north"}],"furniture":[],"schema_version":"0.3"}
```

Observed output:

```
code=0
msg=verdict CONTINUE score=0.4 opening_mismatches=1 extra_apertures=3 moved_objects=0 identity=multiset_fallback/vacuous policy=strict ...
extra_render_apertures=["<non-string:NoneType>->north","<non-string:int>->north","zzz->north"]
```

The input order was `int`, `null`, `"zzz"`; the output order is display-marker lexical order. This is not a false agreement and not a matching-multiset leak, but it is display value influence on the verdict JSON.

## 5. `parse_constant` Coverage Attacks

Source trace:

- `/tmp/review_v06/loop/run_loop_compare.py:165`: `_strict_loads` calls `json.loads(s, parse_constant=_no_const)`.
- `_strict_loads` is used for `packet.json` at line 209, `room.json` at line 252, and the target scene at line 269.
- The only comparator `json.loads` bypass is line 587: `json.loads(text)` in the canonical self-check. That parses comparator-produced text after `canonical(..., allow_nan=False)` has already succeeded; I do not see an input path that can inject `NaN` there.
- `camera.json` is not parsed. See section 6.

Executed raw-token attacks, all from `/tmp/codex_v06_attacks.py`, confirm F3 is closed for parsed documents:

```
f3_target_nested_nan: code=2, REFUSED: target scene: non-finite JSON constant NaN refused (RFC 8259)
f3_target_array_inf: code=2, REFUSED: target scene: non-finite JSON constant Infinity refused (RFC 8259)
f3_target_object_neginf: code=2, REFUSED: target scene: non-finite JSON constant -Infinity refused (RFC 8259)
f3_room_ignored_nan: code=2, REFUSED: room.json: non-finite JSON constant NaN refused (RFC 8259)
f3_room_furniture_inf: code=2, REFUSED: room.json: non-finite JSON constant Infinity refused (RFC 8259)
f3_room_top_neginf: code=2, REFUSED: room.json: non-finite JSON constant -Infinity refused (RFC 8259)
f3_packet_ignored_nan: code=2, REFUSED: packet.json: non-finite JSON constant NaN refused (RFC 8259)
```

Overflow spelling boundary:

- `overflow_target_used_object_bbox` input used `"object_bbox":[1e999,0.6,0.1,0.2]`; observed `code=0`, `CONTINUE score=0.0`, `position_unverified:["o1"]`.
- `overflow_target_ignored_evidence` input used `"evidence":{"score":1e999}`; observed `code=0`, `BELOW_THRESHOLD score=0.0`.

I do not count the ignored-evidence overflow as an F3 break: `1e999` is syntactically valid JSON, and the field is outside comparator semantics. It does mean the "finite checks catch overflow spellings" statement is only true for compared fields, not globally.

## 6. `camera.json` Opacity Assessment

Source assessment: `camera.json` is only read as bytes and sha256-verified in `/tmp/review_v06/loop/run_loop_compare.py:238-249`. It is never decoded, parsed, compared, scored, or used by `_fully_verified`. Its sha is echoed into `input_sha256`, so changing camera bytes changes the verdict artifact hash and `input_sha256.camera_json`, but not the comparator decision or discrepancy fields.

Executed attacks:

```
camera_nan_opaque:
  camera.json = {"x":NaN}\n
  code=0
  msg=verdict BELOW_THRESHOLD score=0.0 opening_mismatches=0 extra_apertures=0 moved_objects=0 identity=exact/exact policy=strict ...
  input_sha256.camera_json=304474eea3b34071a927952d6e595c7989d205be9f0b768d97b34bdbd551997d

camera_null_opaque:
  camera.json = null\n
  code=0
  msg=verdict BELOW_THRESHOLD score=0.0 opening_mismatches=0 extra_apertures=0 moved_objects=0 identity=exact/exact policy=strict ...
  input_sha256.camera_json=38e0b9de817f645c4bec37c0d4a3e58baecccb040f5718dc069a72c7385a0bed
```

Conclusion: opaque camera bytes are inert for the comparator's decision. The remaining gap is contract/documentation precision: `camera.json` is an integrity-checked opaque sidecar, not an RFC-8259 parsed input document.

## 7. Marker-Ambiguity Assessment

Executed cases:

```
f1_marker_generated:
  target category = "<unreadable-category:int>"
  render category = 7
  code=0
  msg=verdict CONTINUE score=0.4 ...
  object_diff={"matched":[],"missing_in_render":["<unreadable-category:int>"],"extra_in_render":["<unreadable-category:int>"],"moved":[]}

f1_marker_literal:
  target category = "<unreadable-category:int>"
  render category = "<unreadable-category:int>"
  code=0
  msg=verdict BELOW_THRESHOLD score=0.0 ...
  object_diff={"matched":["<unreadable-category:int>"],"missing_in_render":[],"extra_in_render":[],"moved":[]}
```

The generated marker cannot collide into a match with the literal target string. The generated-marker case is confusing to a human because `missing` and `extra` contain the same string, but I did not find a way to turn that ambiguity into a changed strict decision.

## 8. Fresh Findings, Determinism, RFC 8259, Mirror Divergence, Test Overclaims

Finding FRESH-1, HIGH: malformed packet member filesystem shapes traceback instead of refusing exit 2.

Source cause:

- `packet.json` read at `/tmp/review_v06/loop/run_loop_compare.py:209` catches `ValueError` but not `OSError`.
- Required packet members are read at lines 238-249 with `p.read_bytes()` and no `OSError` handling.
- `run()` catches `Refused`, `TypeError`, `ValueError`, `KeyError`, `AttributeError`, and `IndexError` at lines 580-583, but not `OSError`.
- Target scene reading does catch `OSError` at lines 267-271, so the asymmetry is packet-side.

Exact filesystem attack script: `/tmp/codex_v06_fs_malformed.py`; full stdout: `/tmp/codex_v06_fs_malformed.out`.

Observed output:

```
CASE target_json_directory
mutation=target.json is a directory
code=2
msg=REFUSED: cannot read target scene: [Errno 21] Is a directory: '/tmp/codex_v06_fs_runs/target_json_directory/target.json'

CASE packet_json_directory
mutation=packet.json is a directory
raised=IsADirectoryError: [Errno 21] Is a directory: '/tmp/codex_v06_fs_runs/packet_json_directory/packet/packet.json'

CASE render_png_directory
mutation=render.png is a directory
raised=IsADirectoryError: [Errno 21] Is a directory: '/tmp/codex_v06_fs_runs/render_png_directory/packet/render.png'

CASE room_json_directory
mutation=room.json is a directory
raised=IsADirectoryError: [Errno 21] Is a directory: '/tmp/codex_v06_fs_runs/room_json_directory/packet/room.json'

CASE camera_json_directory
mutation=camera.json is a directory
raised=IsADirectoryError: [Errno 21] Is a directory: '/tmp/codex_v06_fs_runs/camera_json_directory/packet/camera.json'
```

CLI confirmation for `packet_json_directory`:

```
python3 loop/run_loop_compare.py --target /tmp/codex_v06_fs_runs/packet_json_directory/target.json --packet /tmp/codex_v06_fs_runs/packet_json_directory/packet --out /tmp/codex_v06_fs_runs/packet_json_directory/cli_out --threshold 0.0

exit code: 1
last line: IsADirectoryError: [Errno 21] Is a directory: '/tmp/codex_v06_fs_runs/packet_json_directory/packet/packet.json'
```

This violates the method block refutation rule for malformed input: it should exit 2 with `REFUSED`, not traceback.

Wrong-render strict checks:

```
fresh_wrong_wall: code=0, CONTINUE score=0.2, opening_mismatches=1
fresh_missing_object: code=0, CONTINUE score=0.2, missing_in_render=["chair"]
fresh_moved_object: code=0, CONTINUE score=0.2, moved=[{"object_id":"o1","offset_norm":0.8,...}]
```

I found no wrong render reported as strict `BELOW_THRESHOLD` under the semantic attacks run in this round. The BROKEN verdict is from malformed packet tracebacks and the `_disp` sort-value leak, not from a strict false agreement.

Determinism:

```
determinism_wrong_wall:
  code1=0; code2=0; bytes_equal=True
  sha1=c07418f0be4d4b6c49368ef21a9a93c681bbf768c67e7d3b17a36fdd7d3c1e77
  sha2=c07418f0be4d4b6c49368ef21a9a93c681bbf768c67e7d3b17a36fdd7d3c1e77

determinism_unreadable:
  code1=0; code2=0; bytes_equal=True
  sha1=17b7b36f38d6d80e7c0edd9de193f0022ad64c7957db1b2d307d2dbfc56fa232
  sha2=17b7b36f38d6d80e7c0edd9de193f0022ad64c7957db1b2d307d2dbfc56fa232
```

RFC 8259 output validity:

```
python3 /tmp/codex_v06_output_audit.py
audited=19
failures=0
```

The audit reparsed generated verdicts with `parse_constant` refusal and checked byte-for-byte canonical output.

Mirror divergence from frozen platform:

- The named frozen platform files were not present under `/tmp/review_v06`, so I did not re-run a source-level mirror diff against New_VR_Platform.
- Against the prompt's frozen-rule summary, the comparator still uses `bbox_xywh` for target openings, `object_bbox` for target objects, `ap<i>` source opening indexes, source object ids, and `_source_bbox` for object position checks.
- I found no new divergence inside the loop files. The known schema-versus-fixture conflict in the platform context remains out of this review's scope.

Test overclaims:

- `test_f3_nonfinite_constants_refused_at_parse` says "every input document", but the test covers target, `room.json`, and `packet.json`, not `camera.json`. If camera is called an input document, that wording overclaims; if camera is explicitly documented as opaque, it is only a wording issue.
- No test covers packet-member filesystem read errors such as `packet.json`/`room.json`/`render.png`/`camera.json` being directories, which is the FRESH-1 traceback gap.

## 9. What This Review Did Not Cover

- I did not review or test `loop/orchestrate.py` or `loop/tests/test_orchestrate.py`.
- I did not use the live working tree or any post-hash changes.
- I did not run a real photo/VR production pipeline.
- I did not validate against an external JSON Schema engine.
- I did not audit `--allow-unverified-agreement` as a safety policy beyond confirming strict-mode behavior.
- I did not prove global absence of false agreement; I report no false agreement under the 39 built-in tests plus the 29 custom attack cases in `/tmp/codex_v06_attacks.py`.

## 10. Scratch Script Paths

- `/tmp/codex_v06_attacks.py`
- `/tmp/codex_v06_attacks.out`
- `/tmp/codex_v06_trace.py`
- `/tmp/codex_v06_trace.out`
- `/tmp/codex_v06_output_audit.py`
- `/tmp/codex_v06_output_audit.out`
- `/tmp/codex_v06_fs_malformed.py`
- `/tmp/codex_v06_fs_malformed.out`
- Generated packet/target/output directories: `/tmp/codex_v06_runs` and `/tmp/codex_v06_fs_runs`

## 11. Identity And Separation Level

Reviewer identity: Codex, non-author adversarial reviewer.

Separation level: independent review lane against the pinned committed hash only. I did not author changes to the subject and did not attempt a git commit.
